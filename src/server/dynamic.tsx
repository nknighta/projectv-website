import React ,{useState,useEffect}from "react";

const useDynamicElement = () => {

};

// judge width value
const defaultWidth = () => {};

export default useDynamicElement;

// mobile (under width 890px) => display: none;
// desktop (over width 890px) => no return value;